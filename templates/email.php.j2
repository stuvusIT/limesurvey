<?php  if ( ! defined('BASEPATH')) exit('No direct script access allowed');
// {{ ansible_managed }}
$config = array();
$config['siteadminemail']     = '{{ limesurvey_mail_admin_address }}'; // The default email address of the site administrator
$config['siteadminbounce']    = '{{ limesurvey_mail_bounce_address }}'; // The default email address used for error notification of sent messages for the site administrator (Return-Path)
$config['siteadminname']      = '{{ limesurvey_mail_admin_name }}';      // The name of the site administrator

$config['emailmethod']        = 'smtp';           // The following values can be used:
$config['protocol'] = $config['emailmethod'];
// mail      -  use internal PHP Mailer
// sendmail  -  use Sendmail Mailer
// smtp      -  use SMTP relaying

$config['emailsmtphost']      = '{{ limesurvey_smtp_server }}:{{ limesurvey_smtp_port }}';      // Sets the SMTP host. You can also specify a different port than 25 by using
// this format: [hostname:port] (e.g. 'smtp1.example.com:25').

$config['emailsmtpuser']      = '{{ limesurvey_smtp_user }}';               // SMTP authorisation username - only set this if your server requires authorization - if you set it you HAVE to set a password too
$config['emailsmtppassword']  = '{{ limesurvey_smtp_passwort}}';               // SMTP authorisation password - empty password is not allowed
$config['emailsmtpssl']       = '{{ limesurvey_smtp_ssl }}';               // Set this to 'ssl' or 'tls' to use SSL/TLS for SMTP connection

$config['emailsmtpdebug']     = {{ limesurvey_smtp_debug | int }};                // Settings this to 1 activates SMTP debug mode

$config['maxemails']          = 50;               // The maximum number of emails to send in one go (this is to prevent your mail server or script from timeouting when sending mass mail)

$config['emailcharset']       = "utf-8";

return $config;
